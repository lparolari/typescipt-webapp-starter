{
  "name": "tic-tac-toe",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn parcel serve --public-url $(pwd)/dist index.html",
    "build": "yarn parcel build --public-url $(pwd)/dist index.html ",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "jest": "^26.6.1",
    "parcel-bundler": "^1.12.4",
    "ts-jest": "^26.4.3",
    "typescript": "^4.0.5"
  }
}
